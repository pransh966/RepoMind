services:
  repomind:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/code/data          # persist the FAISS index across restarts
      - ${REPO_TO_INDEX:-./}:/repo # mount a repo you want to index at /repo inside the container
